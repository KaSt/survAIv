build/
sdkconfig.old
*.bin
*.elf
*.map
managed_components/
dependencies.lock
# Auto-fetched LovyanGFX library (cloned on first display board build)
boards/LovyanGFX/

# Private keys / credentials — never commit these
*.pem
*.key
nvs_*.csv
wallet_*.csv
*/sdkconfig
hub/
survaiv.db
